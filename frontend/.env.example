# API base URL
# Use /api when the frontend is served from the same origin as the backend.
# Use a full backend origin when the frontend is hosted separately.
VITE_API_BASE_URL=http://127.0.0.1:5011/api

# Mapbox Access Token (Register at mapbox.com)
VITE_MAPBOX_TOKEN=your_mapbox_token_here
